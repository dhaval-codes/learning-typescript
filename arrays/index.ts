const superHeros: string[] = []
// const heroPower: number[] = []
const heroPower: Array<number> = []

type User = {
    name: string,
    isActive:boolean
}
const allUsers: User[] = []

allUsers.push({name:"DHAVAL", isActive: false})

// suppose we want arrays inside array and so, usually for ML models, we can define them as-

const mlModels: number[][] = [
    [255,212,123],[34,76,129]// rgb values of pixels of a pic for ex. 
]

superHeros.push("spiderman")
heroPower.push(3)

export {} 