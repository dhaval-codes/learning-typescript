enum SeatChoice {
    AISLE = "Aisle",
    MIDDLE = "middle",
    WINDOW = "window",
    FOURTH = 0
}

const djpSeat = SeatChoice.AISLE

// use const before enaum reduces js code into less lines

export {}